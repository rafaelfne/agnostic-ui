/// Renderer SDUI nativo em Flutter (ADR 0005).
///
/// Por ora expõe apenas o modo de renderização; o `TemplateRenderer`, o registry
/// de componentes e o runtime de Dispatcher/FlowEngine chegam em F2/F4.
library;

export 'src/render_mode.dart';
