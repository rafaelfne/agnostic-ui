/**
 * Interface layer (manual, Parte 2.5): controllers and HTTP helpers that
 * translate HTTP ↔ use cases. Route handlers in `app/api/**` resolve a
 * controller from the request container and delegate to it.
 */
export * from './http';
export * from './controllers';
