/** Version of this engine package. */
export const ENGINE_VERSION = '0.0.0';

/**
 * Version of the config schema vocabulary the engine understands. Config
 * artifacts are versioned against this; bumping it implies a config migration
 * (ADR 0002, "Versionamento de schema de config").
 */
export const CONFIG_SCHEMA_VERSION = 1;
