/**
 * Os use cases do vertical financeiro viraram **config** interpretada pelo engine
 * (Fase C, ADR 0002 §8). Não há mais classes hardcoded — os flows vivem em
 * `infra/engine/flows/` e são servidos pela rota catch-all.
 */
export {};
