/// Yuki Labs Playground — app de referência do renderer SDUI nativo (ADR 0005).
library;

export 'main.dart' show PlaygroundApp, PlaygroundHome;
export 'src/mock_documents.dart' show mockDocumentFor;
